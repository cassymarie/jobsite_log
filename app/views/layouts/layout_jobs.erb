<!DOCTYPE html>
  <head>
    <title>Jobsite Log - Jobs</title>
    <link rel="stylesheet" href="/stylesheets/index.css" />
  </head>
  <body>
  <header>
            <div class="dropdown" style="float:left;">
                <button class="dropbtn">Menu</button>
                <div class="dropdown-content" style="left:0;">
                  <% unless @jobsite.nil? %>
                      <a href="/jobsites">Jobsites</a>
                  <% end %>
                  <a href="/logout">Log Out</a>
                </div>
              </div>

              <div  id="headerWelcome"> Welcome, <%=current_user.first_name%> <%=current_user.last_name[0].capitalize%>!</div>
  </header>

    <div class="main_page">

        <h2 id="sub_header">
            <a href="/jobsite/<%=@jobsite.id%>"><%=@jobsite.name%></a>
            <button id="back_button" style="float:right;"><a href="/jobsite/<%= @jobsite.id %>">X</a></button>
        </h2>

        <%= yield %>

        <% if (@jobsite.jobs.size > 0 && @job.nil?) || (@jobsite.jobs.size > 1 && !@job.nil?)%>
            <div id="display_list">   
                <h2>Current Jobs</h2> 
                <table>
                    <tr>
                        <th></th>
                        <th></th>
                        <th>Job No.</th>
                        <th>Job Name</th>
                        <th>Customer</th>
                        <th></th>
                        <%@jobsite.site_areas.each do |area|%>
                            <th><%=area.code%></th>
                        <%end%>
                    </tr>
                    <% @jobsite.jobs.each do |job| %>
                    <tr>
                        <td  style="width:1%" ><button><a href="/jobsite/<%= @jobsite.id %>/jobs/delete/<%= job.id %>">X</a></button></td>
                        <td  style="width:1%" ><button><a href="/jobsite/<%= @jobsite.id %>/jobs/edit/<%= job.id %>">...</a></button></td>
                        <td style="width:5%"><%=job.job_number%></td>
                        <td style="width:20%"><%=job.name%></td>
                        <td style="width:20%"><%=job.customer%></td>
                        <td style="width:.25%" ></td>
                            <% @jobsite.site_areas.each do |area| %>
                                <td style="width:3.5%" ><%='X' if job.areas.include?(area)%></td>
                            <% end %>
                    </tr>
                    <% end %>
                </table>
            </div>
        <%end%>
    </div>
  </body>
</html>