<h1><%= @jobsite.name %></h1>

<h2>Jobs   <button><a href="/jobsite/<%= @jobsite.id %>/jobs/edit">edit</a></button></h2> 
<table>
    <tr>
        <th>Job No.</th>
        <th>Job Name</th>
        <th>Customer</th>
        <th>        </th>
        <%@site_areas.each do |area|%>
        <th><%=area.code%></th>
        <%end%>
    </tr>

    <% @jobs.each do |job| %>
        <tr>
            <td><%=job.job_number%></td>
            <td><%=job.name%></td>
            <td><%=job.customer%></td>
            <td>        </td>
            <% @site_areas.each do |area| %>
                <td><%='X' if job.areas.include?(area)%></td>
            <% end %>
        </tr>
    <% end %>
</table>
<br>
<h2>Site Crew   <button><a href='/sitecrew/<%= @jobsite.id %>/edit'>Edit</a></button> <button><a href='/sitecrew/<%= @jobsite.id %>/new'>Add</a></button></h2>

<table>
    <tr>
        <th>Employee Name</th>
        <th>Title</th>
    </tr>
    <%@employees.each do |employee|%>
    <tr>
        <td><%=employee.full_name%></td>
        <td><%=Title.find(employee.title_id).title%></td>
    </tr>
    <%end%>
</table>